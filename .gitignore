coverage/
coverage.data
*.gem
.bundle
Gemfile.lock
pkg/*
# Added so not to force others to use .rvmrc and .overcommit.yml files.
.rvmrc
.overcommit.yml
# Added gemfiles directory since it is derived by "appraisal install"
gemfiles/
